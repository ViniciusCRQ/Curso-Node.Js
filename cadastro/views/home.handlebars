<nav class="navbar navbar-expand-lg navbar-light bg-white">
  <a class="navbar-brand" href="/"><h1 class="font-weight-light bg-white">#<strong>POSTS</strong></h1></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item ml-3">
        <a class="nav-link" href="/">Home</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/cad">Cadastrar</a>
      </li>
    </ul>
  </div>
</nav>
<table class="table table-hover table-bordered">
        <tr>
            <th scope="col" class="w-25 p-3">ID</th>
            <th scope="col" class="w-25 p-3">Título</th>
            <th scope="col" >Conteúdo</th>
            <th scope="col" class="w-25 p-3">Data de criação</th>
            <th scope="col" >Opções</th>
        </tr>
    </thead>
    <tbody>
{{#each posts}}
    <tr>
        <td>#{{dataValues.id}}</td>
        <td class="w-25 p-3 text-success"><h5>{{dataValues.titulo}}</h5></td>
        <td><p>{{dataValues.conteudo}}</p></td>
        <td><small>{{dataValues.createdAt}}</small></td>
        <td><a href="/deletar/{{dataValues.id}}"><button class="btn btn-outline-success my-2 my-sm-0" type="submit">Deletar</button></a></td>
    </tr>
{{/each}}

    </tbody>
    </table>
<div class="text-center">
    <a href="/cad"><button class="btn bg-success text-white mt-4">Cadastrar novo post</button></a>
</div>