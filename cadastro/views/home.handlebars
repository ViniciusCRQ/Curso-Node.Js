<h1>Posts:</h1> 
<a href="/cad"><button>Cadastrar</button></a>
{{#each posts}}

    <hr>
    <small>{{dataValues.createdAt}}</small>
    <h2>{{dataValues.titulo}}</h2>
    <hr>
    <p>{{dataValues.conteudo}}</p>
    <a href="/deletar/{{dataValues.id}}"><button>Deletar</button></a>
    <hr>

{{/each}}