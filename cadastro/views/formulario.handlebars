<div class="container mt-4">
    <div class="row justify-content-center align-items-center">
        <div class="col-md-6 align-middle">
            <div class="card">
            <form action="/add" method="POST">
                <div class="text-center mt-3">
                <h2 class="text-warning">Cadastrar Post</h2>
                <p>Título : </p>
                <input type="text" name="titulo" class="mt-n3">
                <p class="mt-2">Conteúdo: </p>
                <textarea name="conteudo" class="mt-n2"></textarea>
                <br>
                <button type="submit" name="" class="btn bg-warning text-white mt-2 mb-3">Cadastrar postagens</button>
                </div>
            </form>
            </div>
        </div>
    </div>
</div>
